[workspace]

members = [
    "lipl-axum",
    "lipl-repo-http-server",
    "lipl-fs-repo",
    "lipl-postgres-repo",
    "lipl-types",
    "lipl-upload",
    "parts",
    "rest-api-client",
    "timeit",
]

[profile.release]
lto = true
strip = true
opt-level = "z"
